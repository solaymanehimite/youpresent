export interface FormData {
    firstName: string;
    lastName: string;
    number: string;
    dueDate: Date;

    presentationTitle: string;
    presentationDescription: string;
    slides: number;

    theme: string;
    isAnimated: boolean;
}
