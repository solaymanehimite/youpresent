import { Form } from "@/components/Form";

export default function Home() {
    return (
        <div className="flex min-h-screen items-center justify-center p-10 sm:px-0 bg-zinc-50 font-sans dark:bg-black">
            <Form />
        </div>
    );
}
